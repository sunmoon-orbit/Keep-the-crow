# 参与更新

欢迎补充功能、修正踩坑记录或改进示例。这个仓库来自真实系统的实践，因此发布前最重要的不是文笔，而是把生产数据留在生产环境里。

## 提交前检查

1. 只提交完成教程所需的最少文件；先看 `git status --short` 和 `git diff --cached`。
2. 示例凭据使用 `your_api_key`、`<TOKEN>` 等明显占位符，不使用真实值的截断版。
3. 不提交 `.env`、私钥、keystore、数据库、日志、HAR、崩溃包、导出的对话或记忆文件。
4. 不粘贴真实对话、记忆正文、生产 system prompt 或带正文的调试响应；用虚构且不可反推的最小示例。
5. 路径、域名、IP、账号名若与教程无关就泛化。个人名字可以保留署名，但不要把名字和凭据、地址或私密内容绑在一起。
6. 新增第三方代码或素材时更新 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)，保留上游版权与许可声明。
7. 运行 `git diff --check`，并确认 README 新增的本地链接真实存在。

## 许可证边界

- 教程原创文字：CC BY-NC 4.0。
- Keep the Crow 原创代码示例：MIT。
- 第三方内容：以具体上游文件的条款为准；没有许可证时只引用思路并独立实现。

提交 PR 即表示你有权贡献这些内容，并同意按本仓库相应的文字/代码许可发布。
