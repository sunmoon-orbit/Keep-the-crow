# 25 — 最近一轮稳定性加固：功能会跑之后，怎样不悄悄伤人

功能越多，真正危险的往往不再是“按钮没反应”，而是更隐蔽的失败：接口重试造成二次计费、流结束却没正文、裁剪切断工具调用、草稿串到别的窗口、更新成功但仍加载旧资源。本篇记录一轮集中加固中最值得复用的做法。

---

## 一、可能重复收费的重试，必须先征得同意

LLM 请求断线时，客户端无法仅凭网络错误判断服务端有没有生成并计费。自动重试可能让用户为同一条消息付两次钱。

把错误分两类：

- **明确未送达**：本地参数校验失败、用户取消、连接建立前失败，可以安全重试。
- **结果不确定**：超时、流中断、网关 5xx、已经收到部分内容。重试前弹确认，文案明确说明“上一次可能已经计费”。

确认框要跟随主题色，用项目已有的描边 SVG，不要突然退回浏览器 `confirm()` 或系统白底方框。取消后保留已收到的内容和错误信息；确认才发起新请求。

**防级联原则**：兼容 OpenAI 中转的降级链（去掉 tools、stream options 等）每一层最多执行一次，并维护已尝试集合。一次请求的失败不能同时触发“参数降级重试”和“网络重试”两套循环。

---

## 二、保存响应诊断，但别保存秘密

“接口 200、usage 也有、正文却为空”如果只显示“生成失败”，几乎没法排查。每条失败消息可以保存一份**最小诊断摘要**：

- provider、model、HTTP 状态、request id
- 流事件种类及数量
- finish/stop reason
- prompt、completion、cache read/write token 数
- 是否收到正文、thinking、tool call

不要保存：Authorization 头、API key、完整请求体、system prompt、记忆注入、用户原文、模型原文。原始响应若必须临时抓取，应只在本地开发开关下启用，短期留存，用完删除。

流解析要兼容 CRLF、同一网络块里的多条 SSE、最后一条没有空行，以及 `[DONE]` 前的 usage 事件。流正常结束但正文为空时，不要制造一条看似成功的空 AI 消息；保留可删除、不进入 L0、下一轮不进上下文的错误气泡。

---

## 三、上下文限制按“逻辑轮”算，不按数组长度算

一次用户提问可能在存储里展开成：用户消息、工具调用、工具结果、模型回复。简单 `slice(-N)` 会把工具调用和结果从中间切开，也可能把 `N` 条误当成 `N` 轮。

正确做法是先把消息归组为逻辑轮：以真实用户消息为边界，把其后的 assistant/tool 链一起归入本轮；裁剪时整轮保留或整轮移除。系统注入、隐藏触发和错误气泡不计入用户轮数。

同时对设置值做上下界保护。`max_tokens` 不仅要小于模型上限，还要为输入上下文留空间；过大的旧配置在模型切换后应自动夹紧并给出可见提示，而不是等 API 返回含糊的 400。

---

## 四、缓存断点要稳定，也要合法

Anthropic 可手动标缓存断点，OpenAI 兼容端点多依赖服务端前缀缓存，但共同原则都是：**稳定前缀不能每轮变形**。

- 动态时间、情绪、检索记忆放在最后一条用户消息之后的动态块。
- 缓存断点打在完整逻辑轮的尾部，不要打在 tool call 与 tool result 中间。
- 给请求对象的深拷贝加标记，不把 `cache_control` 写回持久化消息。
- 锚定裁剪切点，避免每轮向前滑一条导致前缀全毁。
- 用 usage 的 cache read/write 字段验证，不凭“响应快了”猜命中。

缓存优化必须和上下文裁剪一起测试。二者各自正确，不代表组合后仍正确。

---

## 五、草稿按窗口隔离，长聊天分批渲染

全局只有一个 `draft` 时，从 A 窗口切到 B 窗口会把未发送文字一起带过去。草稿应以 `chatId` 为键保存，发送成功后只清当前窗口；删除窗口时清对应草稿。系统分享进来的文字也必须写入当前窗口的草稿通道，不能绕过它直接改 DOM。

长聊天的卡顿通常不在请求，而在 Markdown、高亮、图片和特效一次性挂载。可先渲染最近一段，再用 `requestIdleCallback` 或小批次逐步补旧消息；用户向上滚到边界时继续加载。新增消息始终立即出现，分批任务在切窗口时取消，避免旧窗口内容串回来。

颜文字不要误判成 HTML。只有经过明确允许的标签才进入富文本解析，其余一律按文本转义。

---

## 六、远程 MCP 的最小安全模型

远程 MCP 不是“填个 URL 就把所有工具给模型”：

- 远程只许 HTTPS，本机回环地址可用 HTTP。
- 凭据放独立 token 字段，禁止写进 URL。
- 新发现工具默认关闭；写工具还要服务级二次授权。
- 限制每轮暴露的工具数，调用设超时，结果设长度上限。
- 工具名做稳定映射，包含服务 id 与短哈希，避免同名冲突。
- 图片/音频结果若当前模型链路不能消费，就明确降级成说明，不能把 base64 塞进文本。

工具的 `readOnlyHint` 是上游声明，不是密码学保证。高风险写操作仍应在自己的服务端做权限与审计。

---

## 七、原生壳更新：版本、资源和下载是三件事

在线壳常见错觉是“APK 已升级，所以页面也升级了”。实际上至少有三层缓存：安装包版本、WebView 静态资源、Service Worker/PWA 缓存。

升级时：

1. 用系统 API 安全读取已安装 `versionCode`，解析失败不能当成 0。
2. 只缓存成功取得的远端 release；404、超时和空结果表示“不知道”，不能缓存成“无更新”。
3. 检测原生版本变化后刷新或清理受控的 Web 资源缓存，并保留离线兜底。
4. 生成文件下载走原生下载能力或明确处理 WebView 权限、文件名和 MIME；不能只在桌面浏览器测 `<a download>`。

横屏和平板也不能只是把手机布局拉宽。给内容区设可读最大宽度，让侧栏、输入框、弹窗和通话页分别定义中大屏断点；至少实机测一次旋转、键盘弹起和分屏。

---

## 八、主题收口：差异要明确，细节要同源

黑白主题里两边都用偏白气泡，在白背景上会失去角色区分。与其把 AI 回复涂黑，更适合让**用户气泡使用黑色、白字**，AI 保持纸张般的浅色：阅读长回复更轻，发言归属也一眼可见。

主题新增后做三次扫描：

1. grep 旧主题硬编码色，包括 JSX 内联 style。
2. 对照所有 `var(--token)` 是否有定义。
3. 检查原生小组件、拼音键盘、错误/重试弹框是否读取同一套主题色与透明度。

装饰优先用项目已有风格的 SVG 线条和低透明度几何形，不用 emoji 充当图标。装饰层必须 `pointer-events: none`，并尊重减弱动效设置。

---

## 上线前的最小验收清单

- [ ] 断网、超时、流中断分别测一次；不发生未经确认的收费重试
- [ ] 空正文能留下不污染上下文的诊断错误
- [ ] 工具调用链不会被上下文裁剪截断
- [ ] 缓存 usage 在跨轮对话中真实命中
- [ ] 两个窗口来回切换，草稿互不串线
- [ ] 主动消息和主动来电的开关、冷却、上限、dry-run 都真测
- [ ] MCP 写工具默认拒绝，HTTPS/token/CORS 错误可见
- [ ] APK 升级后版本与 Web 资源都更新，文件能在真机下载
- [ ] 手机竖屏、横屏、平板/分屏各走一次核心流程
- [ ] 发布前执行凭据扫描，并人工确认没有真实对话、记忆导出、生产 prompt 或日志样本
