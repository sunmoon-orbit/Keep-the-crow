# 26 — 小服务器上的可靠协作与上线：worktree、备份、云构建与可恢复操作

功能写出来只是第一步。真正上线时，最容易出问题的往往是工程环境：生产目录本来就有运行状态文件、服务器内存不足、两个编码代理互相抢资源、原生依赖无法在隔离目录加载、构建产物与主分支冲突，或者一次“清理测试窗口”误删了真实历史。

本篇把这些问题整理成一条适合小型 VPS 的可靠工作流。目标不是追求仪式感，而是把每个高风险动作变成可停止、可验证、可恢复的步骤。

---

## 一、小服务器不要同时养两只工程代理

2 核 2G 一类的小服务器可以运行 Node.js 服务和轻量数据库，但不适合同时常驻多个编码代理，再叠加前端构建、Android 编译或原生模块重建。

建议：

- 同一时间只运行一个编码代理。
- 正在提供服务的生产进程交给 PM2/systemd，不与开发任务混在同一终端。
- Web 与 Android 构建优先放到 GitHub Actions 等隔离环境。
- 不为了让测试“全绿”就在生产机临时安装、升级或重建依赖。
- 遇到内存不足时先缩小任务范围，不用更高推理档位硬顶。

模型档位也应按任务选：窄范围 UI、文档或单接口修改通常用中档即可；跨数据库迁移、并发投递和兼容性审查再考虑高档。把额度留给真正需要推理的地方。

---

## 二、生产目录脏，不等于先把它擦干净

运行中的仓库常有状态文件、缓存或未跟踪构建目录。它们可能属于服务，不属于本次开发。不要用 reset、stash 或“顺手提交”把未知状态清掉。

先只读检查：

```bash
git status --short --branch
git worktree list
git rev-parse HEAD
```

然后从最新远端基线创建独立 worktree：

```bash
git fetch origin
git worktree add -b fix/example \
  "$HOME/codex-work/example" origin/main
```

独立 worktree 的好处：

- 不触碰生产目录已有修改。
- 功能分支与生产分支边界清晰。
- 审查、测试、提交都能限定在明确文件集合。
- 任务中断后仍可恢复，不把工作放在容易清理的临时目录。

创建前先检查目标路径和分支是否已存在；共享 Git 元数据需要写权限时，只授权精确的 `git worktree add`，不要把整个家目录设为可信。

---

## 三、提交前先做“文件集合审计”

安全审计不必每次发展成几十轮 review，但提交前至少要确认：**这次到底会提交什么**。

```bash
git status --short
git diff --check
git diff --name-only
git diff --cached --name-only
```

推荐先列出允许提交的文件，再精确 `git add -- <files>`。不要使用范围不明的 `git add .`。

最低隐私检查：

- API key、Bearer token、OAuth 凭据、私钥和 keystore。
- `.env`、数据库、日志、备份、HAR、崩溃包。
- 真实对话、记忆正文、system prompt、推送正文。
- 个人邮箱、手机号、IP、域名、用户名和绝对路径。
- 测试 fixture 是否像真实凭据，是否可能造成误报或泄漏。

示例统一用 `<TOKEN>`、`https://example.invalid`、`/srv/app` 等不可用占位符。日志只保留事件名、实体 id、错误类别与计数，不记录正文。

---

## 四、数据库上线前做一致性快照

SQLite 开启 WAL 后，直接复制主数据库文件不一定得到一致快照。上线前应使用 SQLite 自己的备份能力，例如 `VACUUM INTO` 或 Backup API。

```sql
VACUUM INTO '/srv/app/backups/predeploy-YYYYMMDDTHHMMSSZ.db';
```

备份完成只检查元数据：

- 文件存在且非空。
- 权限为 `0600`。
- 属主正确。
- 路径是新时间戳文件，不覆盖旧备份。

不要为了“验证备份”把真实表内容打印到终端。若要还原演练，使用隔离副本和合成查询，不连接生产服务。

---

## 五、生产合并只允许快进

功能分支经过测试和审查后，生产分支优先使用：

```bash
git merge --ff-only feature-branch
```

如果不能 fast-forward，先停下理解分支关系；不要自动改成普通 merge、rebase 或 force push。

推荐部署顺序：

1. 核对功能分支 commit 与远端一致。
2. 创建数据库一致性备份。
3. `ff-only` 合并到生产分支。
4. 使用生产已有依赖运行完整测试。
5. 只重启目标服务的精确名称。
6. 检查健康接口与安全错误类别。
7. 普通非强制 push 生产分支。
8. 再次核对本地、upstream 与远端 hash。

PM2 操作要写精确服务名，禁止通配符和“重启全部”。健康检查只读取公开状态字段；日志检查优先输出分类计数，不回显可能包含用户正文的原始行。

---

## 六、VPS 构建不了，就让 CI 构建

隔离 worktree 没有依赖，或生产 `node_modules` 来自较旧 lockfile 时，借用旧依赖得到的 build 失败并不能证明代码有错。

不要在小服务器上临时 `npm install` 把依赖树搅乱。更稳妥的做法是：

- 本地运行不依赖缺失包的定向测试、语法检查和 `git diff --check`。
- 提交并推送 feature branch。
- GitHub Actions 按该分支 lockfile 安装依赖并执行测试/build。
- Web、Android 与原生模块分别使用匹配的 Node/JDK/SDK 环境。
- CI 不读取生产密钥，不发布 Release，不自动部署，除非任务明确需要。

CI 绿灯证明“这个 commit 在声明的干净环境中可构建”，比在资源紧张的生产机上强行补依赖更可信。

---

## 七、自动生成目录冲突：保留主分支，再重新生成

如果 workflow 会把 `dist/`、`web/` 等生成物提交回分支，主分支更新后很容易出现 hashed asset 的 rename/rename、rename/delete 和入口 HTML 冲突。

不要手工编辑压缩后的 bundle。处理原则：

1. 确认冲突全部位于已知自动生成目录。
2. 若任何源文件也冲突，立即停止，单独审查。
3. 在 feature 分支合入最新 `origin/main`。
4. 将整个生成目录恢复为 `origin/main` 的精确版本。
5. 确认 feature 源文件仍保持原修改，且未解决冲突为空。
6. 创建普通 merge commit并非强制 push。
7. 在 feature 分支重新运行构建 workflow。
8. 用新生成产物完成 PR 合并。

这样不是“丢掉功能构建”，而是先清除两套旧产物的冲突，再基于最新源代码重新生成唯一正确的一套。

---

## 八、删除测试窗口应当可恢复

聊天与记忆系统中的“删除”风险很高。UI 上看似测试窗口，后台可能已经被引用、批注或重新同步。更安全的模型是 tombstone：

- 以稳定 `conversation_id` 建立移除记录。
- 搜索、列表和详情统一排除活动 tombstone。
- 原始 conversation、messages、批注与书签不物理删除。
- 重新导入或同步不能让已移除窗口自动复活。
- 提供幂等 remove/restore 接口。
- 请求必须带明确的 `confirm: true`。
- 响应只返回 id、状态和时间，不返回标题或正文。

前端所有入口必须使用同一语义。若 L0 搜索已经隐藏，但“共读旧对话”仍显示，说明另一个列表没有接入 tombstone 过滤。正确修法是复用同一服务端状态与确认卡，而不是按“测试”“新对话”等标题写特殊规则。

---

## 九、主动消息与来电要做受控端到端联调

概率调度不适合第一次验收。先做不会随机扩散的受控测试：

### 来电

1. 确认目标 app 有独立 FCM token，不能借用另一个 app 的 token。
2. 创建恰好一条测试 invite。
3. 推送包含兼容字段与稳定窗口标识的 payload。
4. 熄屏验证全屏来电。
5. 分别测试拒接与接听。
6. 按已知 call id 只读核验后端状态与回答时间。
7. 每一步成功后才进行下一通，不连续拨打。

### 主动消息

1. 绑定明确的 conversation id 与 external id。
2. 创建恰好一条合成测试消息，不调用 LLM。
3. 携带稳定 proactive id，避免投递重试造成重复插入。
4. 用户停留在另一个窗口，确认消息不会强行切走当前界面。
5. 点击通知后应进入关联窗口。
6. 目标窗口只插入一条消息。

测试日志只报告 id、状态与安全错误类别，不读取真实对话正文。

---

## 十、FCM 注册最容易卡在启动时序

原生 token 通常异步取得。如果 Web 页面启动时只尝试读取一次，token 尚未准备好就会静默退出；数据库里于是只有其他 app 的 token，看起来像“推送坏了”。

避免方法：

- 原生 `onNewToken` 保存 token 后主动通知 Web 层或安排补传。
- 页面启动后做有界重试，而不是只读一次。
- 手动开启推送时等待 token，并显示明确成功/失败状态。
- 上传时显式携带 app 标识；后端仍需校验允许值。
- 重装、清数据、token 轮换和升级后都应重新注册。
- 发送前按 app 分类计数；目标不存在就停止，绝不借用其他 app token。

启动时序问题可以延期优化，但受控联调前必须先确认目标 token 已成功注册。

---

## 最小验收清单

- [ ] 开发在独立 worktree，生产目录原状态未被触碰
- [ ] staged 文件集合精确，隐私与凭据扫描无命中
- [ ] 数据库已做一致性时间戳备份，权限为 0600
- [ ] 完整测试使用已有依赖通过，lockfile 未变化
- [ ] CI 在干净环境中完成 Web/Android 构建
- [ ] 生成物冲突没有手改 bundle，而是重新构建
- [ ] L0、共读列表和详情统一尊重可恢复移除状态
- [ ] 主动来电的熄屏、拒接、接听和后端回传分别通过
- [ ] 主动消息只进入关联窗口，不抢当前窗口、不重复
- [ ] FCM token 按 app 注册，不借用其他应用目标
- [ ] 只重启目标服务，健康检查正常，安全错误类别为 0
- [ ] 本地、upstream 与远端 commit hash 一致

这套流程看起来比“直接改、直接重启”多几步，但每一步都有清晰停止点。真正节省的不是命令数量，而是避免一次误删、一次错误推送或一次生产依赖污染之后的漫长恢复。
