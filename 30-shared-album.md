# 30 — AI 共同相册：图片、来源和回忆要一起活下来

## 先问清楚：你要的是图床，还是相册

图床解决「给我一个 URL」；相册还要回答：这是什么、谁收藏的、从哪里来、为什么留下、删了能不能后悔。

所以数据模型不能只有 `filename`：

```sql
CREATE TABLE album_photos (
  id INTEGER PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  author TEXT NOT NULL,
  source TEXT NOT NULL,       -- upload / public-library / conversation / agent
  source_url TEXT NOT NULL,
  created_at TEXT NOT NULL,
  image_file TEXT NOT NULL,
  thumb_file TEXT NOT NULL,
  mime TEXT NOT NULL,
  width INTEGER NOT NULL,
  height INTEGER NOT NULL,
  bytes INTEGER NOT NULL,
  fingerprint TEXT NOT NULL,
  deleted_at TEXT
);
```

## 存储路线让读者自己选

| 路线 | 适合 | 优点 | 代价 |
|---|---|---|---|
| SQLite 存元数据 + 本地文件 | 单机 VPS、小相册 | 简单、可整体备份、不依赖云厂商 | 要自己做容量上限和文件一致性 |
| S3 兼容对象存储 + 数据库 | 多机、大量图片 | CDN/扩容成熟 | 多一套凭据、账单和生命周期配置 |
| 只存外链 | 临时收藏、不在意失效 | 不占本地空间 | 链接会死、来源可改图，无法保证回忆还原 |

小型私有项目推荐第一条。不要把大图 base64 塞进主记忆库：备份、VACUUM、列表查询和 Git 分块都会被它拖累。相册用独立 DB 和独立目录，不让一次图片迁移碰主记忆。

## 入库管线：先解码，再统一转码

无论图片来自上传还是公开资料库，都走同一条管线：

1. 请求体和解码后字节都限大小；
2. 检查真实格式，不只信 MIME；
3. 限制解码像素总数，防解压炸弹；
4. 自动旋转，缩到一个合理的最大边，静态化动图；
5. 普通照片转 JPEG，需要保真文字的聊天卡转 PNG；
6. 另生成小缩略图，列表不传原图；
7. 只给服务账号读写权限，随机文件名，不使用用户标题当路径。

低内存 VPS 还要限制并发图片解码。四个大图同时进 sharp，存储还没满，进程先被 OOM 杀了。宁可在忙时回 `429`，也不要拿整个服务赌一张图。

## 远程图片：先处理 SSRF，再谈便利

接受 `image_url` 意味着服务器会代用户发请求。安全的下载器要：

- 只接受 HTTPS，禁用 URL 里的 username/password；
- DNS 解析后拒绝 loopback、私网、link-local、保留地址，IPv4/IPv6 都查；
- 禁止或逐步重验 redirect，否则公网 URL 可以 302 到内网；
- 限超时、Content-Length 和实际读取字节；
- 检查响应 MIME，再用图片解码器二次确认。

如果只从 Wikimedia Commons 之类资料库带图回来，更小的边界是：服务器自己搜索，只返回它验证过的图片域名和来源页。不要让模型凭空编一个图片 URL。

## 来源与许可是数据，不是描述里的一句话

公开图片至少保存：

- 原始来源页，不是只存 CDN 图片直链；
- 作者/摄影者；
- 许可名称和许可 URL；
- 收藏当时的描述。

不要在自己的搜索结果里过度清洗 HTML 到只剩空白；也不要直接把资料库的 HTML 描述渲染进前端。它是外部输入，服务器变成纯文本，前端再按文本展示。

## 对话卡片不是截屏

收藏一段对话有两条路：

| 路线 | 优点 | 问题 |
|---|---|---|
| 真实屏幕截图 | 所见即所得 | 容易带入状态栏、输入框、隐藏思考或无关消息；不同屏幕尺寸不一致 |
| 从消息数据重绘 | 可选精确消息，样式可控，不包含隐藏内容 | 要处理角色方向、头像、换行、表情和长文截断 |

推荐重绘。但要先定义视角：「我的话在右边」里的「我」是当时收藏者，不是数据库里固定的 assistant。同一段对话从不同人的入口收藏，左右方向可以相反。

头像是相册卡片的渲染依赖，不要假设后端能读到浏览器 localStorage。可选：

- 生成卡片时随请求上传当前头像；
- 头像更改时单独同步到相册服务；
- 卡片完全不使用头像，以避免这层耦合。

第二条适合重复生成，但同步失败不能阻止用户正常聊天。

## 去重、配额与并发

指纹不要只 hash 原图：同一张图作为不同回忆，标题、描述和来源可能不同。一个实用的去重键是：

```js
sha256(normalizedImage + JSON.stringify([
  title, description, author, source, sourceUrl
]))
```

容量配额计入原图和缩略图。并发上传时，所有 `await` 完成后再用同步的「查配额 + 插入」临界区，或者用事务/互斥锁，否则两个请求都看到「还有空间」，加起来却超额。

## 删除是收藏系统里最该可后悔的操作

`DELETE` 只写 `deleted_at`，默认列表和媒体路由不再显示；回收站可恢复；超过保留期才由定时清理真正删行和文件。

清理顺序要小心：先确定过期行，在事务里删数据库，再删那些行精确引用的文件。不要用目录 glob 猜哪些文件该删。

## 备份必须同时包含 DB 和文件

只备份 SQLite，还原后得到一本所有照片都碎掉的目录；只备份图片，则丢掉描述、来源和顺序。

稳定快照流程：

1. 先执行过期软删除清理；
2. SQLite 用 `VACUUM INTO` 生成一致副本，不直接 `cp` 活跃 WAL 数据库；
3. 从快照 DB 查出它引用的精确文件名，只复制这些；
4. 打 `tar.gz`，如需推 Git 则分成低于平台上限的块；
5. 还原演练里解包，跑 `PRAGMA integrity_check`，再抽查每行的原图/缩图都存在。

备份任务不要直接在工作树里改分块然后 `git add .`。用独立临时快照目录或专用 worktree，显式添加备份目标，避免把正在开发的文件和秘密一起推上去。

## 验证清单

- [ ] 先选本地文件、对象存储或外链，把失效/备份成本说清楚
- [ ] 相册与主记忆库分库、分目录、分配额
- [ ] 限请求体、解码字节、像素和并发数
- [ ] 远程下载拒绝私网、redirect 绕过、超时和超大响应
- [ ] 公开图片保存作者、许可和来源页
- [ ] 对话卡片明确收藏者视角，不包含隐藏思考
- [ ] 删除先进回收站，后台延迟硬删
- [ ] 备份中 DB 和 DB 引用的文件是同一时点的集合
- [ ] 在新目录做过一次真正还原，不只看压缩包能打开
