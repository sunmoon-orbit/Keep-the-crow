# 28 — 多模型调用可靠性：别让一条消息在中转站里消失

## 这一章解决什么

聊天前端同时接 Anthropic、OpenAI 兼容端点和 Gemini 后，难点不是「发一个 `fetch`」，而是你永远不知道中间那层做了什么：

- HTTP 200，但正文为空；
- SSE 收到一半就断，上游却已经计费；
- 兼容站认得 `messages`，不认 `prompt_cache_key` 或 `tools`；
- 模型连续只调工具，六轮过去没写一句话；
- 一次挂几十个工具，中转站把请求或回复截断。

这里给的不是某家 API 的专用封装，而是一套「不把故障伪装成正常」的出口设计。

## 先选路线

| 路线 | 适合 | 优点 | 代价 |
|---|---|---|---|
| 前端直连各家 API | 单人私用、key 存在本机 | 结构简单，少一层故障 | CORS/链路差异都落在前端，key 在客户端 |
| 自建统一网关 | 多端共用、需审计/限流 | key 不下发，可统一协议 | 多一个需要监控的服务，不能再靠浏览器调试猜问题 |
| 第三方 OpenAI 兼容中转 | 模型来源多、希望一个账单 | 接入快 | “兼容”常只覆盖最小公分母，错误也可能被改写 |

本章建议：无论选哪条，都把下面的流完整性、诊断、工具上限和重试策略放在**最靠近请求出口**的地方。

## SSE 结束不等于回复完整

最常见的错误是「读取器 `done: true` 就当成成功」。TCP 断开、CDN 超时和正常 `[DONE]` 都会让读取结束，它们不是一回事。

解析器至少要记住：

```js
{
  text,
  streamComplete,       // 是否看到协议级结束事件
  finishReason,
  badEventCount,        // 多少个 SSE 事件 JSON 解析失败
  usage,
  responseDiagnostic,
}
```

已经吐出部分正文时，不要把它整段丢掉；保留部分文字，再在末尾标记「这条未完整」。一个坏 SSE 事件也不能 `catch {}` 静默吞掉，否则用户会把缺字回复当成原话。

## 空回不是一种正常回复

HTTP 200 但正文为空时，按结束原因分类：

- `length` / `max_tokens`：输出额度被思考或工具参数用完；
- `content_filter` / `safety` / `refusal`：上游拦截；
- `tool_calls` / `tool_use`：工具循环没有收尾；
- 没有 finish reason，也没有协议结束事件：更像链路中断。

错误气泡要和正常助手消息分开：可删、不写进对话存档、不进下一轮上下文。否则一个网络故障会成为模型的「旧对话事实」。

## 重试要克制，还要保留第一现场

自动重试只适合能证明「上一次没有造成外部状态」的场景。已经可能执行了写工具、发了消息或完成计费时，重试会制造双写。

实用规则：

1. `429` 不在后台偷偷重试；
2. 只有错误正文明确点名不兼容字段时，才删掉该字段重试一次；
3. 重试失败时，诊断里同时保留首次响应，不要用第二个错误把真因覆盖；
4. 已经吐过文字再断流，由用户确认是否重发。

## 工具太多：两条路线

### 路线 A：设置页让人手工勾选

适合工具数量不大、用户知道自己在做什么的系统。优点是实现简单、权限显式；缺点是用户要先预知模型将用什么。

### 路线 B：按需「工具抽屉」

工具超过一个可控阈值时，首轮只发几个核心工具和一个路由工具：

```js
const openToolbox = {
  name: 'open_toolbox',
  description: '打开当前任务需要的工具组，下一轮出现真正工具',
  parameters: {
    type: 'object',
    properties: {
      groups: { type: 'array', items: { enum: ['books', 'life', 'album', 'mcp'] }, maxItems: 2 }
    },
    required: ['groups']
  }
}
```

执行 `open_toolbox` 不做业务写入，只更改下一轮广告给模型的工具集。每条用户消息重建一个新抽屉，不把上一次已加载的组永久带着。

这条路会偶尔多一次模型请求，但它避免每轮都携带几十份 JSON Schema。按 token 计费时通常更省；按请求次数计费时要自己衡量。

## 远程 MCP：「能发现」不等于「允许执行」

让用户粘贴一个 MCP URL 后，系统会多出三层权限：

1. **连接层**：谁保存 bearer/OAuth token，谁去请求远程服务；
2. **暴露层**：发现到的哪些工具会告诉模型；
3. **执行层**：哪些工具只读，哪些可能改变外部状态。

有两条接入路线：

| 路线 | 适合 | 代价 |
|---|---|---|
| 浏览器直连 MCP | 内网、无凭据、服务端 CORS 配合 | token 在浏览器，CORS/OAuth 回调和分应用代理都可能卡住 |
| 自己的后端代连 | 跨域、需凭据托管、手机端 | 后端成为 SSRF 和凭据保管边界，必须加固 |

后端代连时，凭据只存服务端，列表/备份/错误日志中都不返回。URL 要拒绝用户信息段和内网目标，跟随 redirect 时每一跳重新验证。OAuth 的 authorization code、device code 和 refresh token 是三种不同生命周期，不要塞进一个「凭据」字符串里猜。

发现工具后默认全关，由用户逐个开启，并设一个全局数量上限。名称要转成 provider 安全的稳定 wire name，不能只截断：两个长名可能截成同一个。一个可实践的形状是：

```text
mcp_<server-short-id>_<tool-slug>_<hash(server-id + original-name)>
```

执行前再根据服务器 id + 原工具名反查，不相信模型传回的 wire name 就是授权。只有 `readOnlyHint: true` 的工具可默认进执行层；未标注和明确写工具都经过「允许该服务写外部状态」独立闸门。

最后，MCP 返回可能同时包含 text、structured content、resource link 和二进制块。交给模型前先做一次归一化和限长；不要把整块 base64 图片塞回文本上下文。

## 工具循环的最后一轮必须没有工具

单纯写 `for (let i = 0; i < 6; i++)` 不叫收尾策略。如果第六轮仍然携带工具，模型完全可以继续调用；循环然后直接结束，用户就只收到「空回」。

把最后一轮保留为收尾轮：

```js
const forceFinalize = i === MAX_REQUESTS - 1
const roundTools = forceFinalize ? [] : drawer.getTools()
if (roundTools.length) body.tools = formatTools(roundTools)
if (forceFinalize) messages.push({
  role: 'user',
  content: '工具阶段已结束，请根据现有结果直接写完整正文。'
})
```

注意是**不发 `tools` 字段**，不是发 `tools: []`。有些兼容站连空数组都不接受。

最后一轮无工具解决了「一直调用、不肯说话」，但还要防另一面：多步任务可能在提交写操作前就撞上收尾轮。例如旅行链路先开抽屉、开门、观察、搜索，真正的 `save_*` 已经排到上限之外。更稳妥的办法是给已经开始、尚未提交的写链路一个**按需提交宽限轮**：只额外暴露目标写工具，执行后再进入无工具收尾。

宽限轮不能靠工具名字被调用过就判成功。执行器要记录结构化回执；只有 `saved:true`、新记录 id 或服务定义的等价成功状态才能清除 pending。若最终仍 pending，应用层必须把「尚未写入」写进可见结果，不能让模型用自然语言自行宣布成功。

## 诊断不能泄密

可以保存：provider、HTTP 状态码、finish reason、工具数量、消息数量、模型名、裁剪后的上游错误。

不要保存：Authorization header、API key、完整请求体、用户对话正文、工具返回的私密数据。

上线前可用一个假 key 和假对话制造 400/429/断流，然后全仓搜索这些哨兵字符串，确认它们没有进日志、诊断和消息历史。

## 验证清单

- [ ] 没看到协议结束事件时，不把读取器关闭当成完整回复
- [ ] 空回按 length / safety / tool loop / connection 分类
- [ ] 429 不静默重试；字段兼容回退最多一次
- [ ] 错误气泡不进对话存档与下一轮上下文
- [ ] 工具少时直发，工具多时手动勾选或按需加载
- [ ] 远程 MCP 的连接、暴露和执行是三道独立权限闸
- [ ] 最后一轮不带工具，强制收尾
- [ ] 多步写入在收尾前有按需提交轮，且只认执行层成功回执
- [ ] 诊断只留请求轮廓，不留密钥和私密正文
