# 24 · 塔罗：把一个独立小工具搬进主程序

> 起因是一句需求：「我只会抽塔罗，但是我不会解牌。能不能干脆搬到言叽里，
> 这样你就可以自己抽，或者我抽完直接让你帮我解牌？」

功能本身不难：78 张牌、三种牌阵、随机抽。真正值钱的是搬运过程中撞出来的四件事，
它们跟塔罗没关系，跟**任何「同一份数据被多个地方消费」的功能**都有关系。

---

## 一、先问清楚三件事，再动手

搬一个已经在用的工具，最容易犯的错是「顺手升级」——把旧的那个也改了、
把交互也改「更好」了。结果她原本用得好好的东西变了样。

所以动手前只问了三个二选一：

| 问题 | 她选的 | 为什么这个答案改变了实现 |
|------|--------|--------------------------|
| 独立小工具留不留？ | **留，两边都能抽** | 于是必须解决「两份牌义会不会长歪」——见第二节 |
| 抽完自动把牌发给 AI 吗？ | **不要，点一下才发** | 于是需要一个显式按钮，而不是抽完就注入 |
| 抽牌记录存不存？ | **存** | 于是需要一张表，而「存什么」成了第三节的问题 |

第二个答案值得多说一句。默认设计会做成「抽完自动解牌」，因为看起来更顺。
但她的原话是不想被打断自己琢磨的过程。**AI 主动介入的时机，往往该由用户按一下来定义，
而不是由「什么时候技术上可以」来定义。**

---

## 二、单一数据源，和「漂移会不会报错」

牌义有三个消费方：独立小工具、主程序前端、服务端抽牌接口。

最省事的做法是复制三份 JSON。这一定会坏——不是可能会，是一定：
改一句牌义，总有一边忘了同步，而且**不报错，只是悄悄不一样**。
（同一天早上在另一份配置上刚踩过一模一样的坑。）

最干净的做法是让独立小工具也去读同一个 JSON，做成真正的单一数据源。
没这么做是有具体理由的：它是个能离线用的单文件页面，改成异步读外部 JSON
就给它引入了一个「加载失败就打不开」的新故障模式——而漂移这个风险，
有更便宜的办法治。

所以退一步，采用**生成物 + 校验模式**：

```
index.html （唯一事实来源，牌义写在里面）
    │  node extract-cards.js
    ├──→ cards.json                       （规范位置，也是校验基准）
    ├──→ 前端 public/tarot-cards.json      （打进构建产物，离线可用）
    └──→ 服务端 assets/tarot-cards.json    （抽牌接口读它）
```

关键不是那个生成脚本，而是它的 `--check` 模式：

```bash
node extract-cards.js           # 重新生成
node extract-cards.js --check   # 只核对，不一致就非零退出
```

> **「改完忘了重新生成」从一件会悄悄发生的事，变成一件会报错的事。**
> 这是这一节唯一需要记住的话。

### 提取实现的两个细节

**用求值，不要用正则。** 牌义正文里有中文引号、破折号、省略号，正则解析迟早在某张牌上崩掉：

```js
const vm = require('vm');
const ctx = {};
vm.createContext(ctx);
vm.runInContext(`${block}\n;__out = { cards: ALL_CARDS, spreads: SPREADS };`, ctx);
```

求值拿到的就是浏览器实际看到的那份数据，不存在「解析器理解得对不对」的问题。

**按标记行定位，不要按行号切块。** 源文件一改行数就错位：

```js
const startIdx = lines.findIndex((l) => l.trim().startsWith('const MAJOR_ARCANA = ['));
```

**生成完立刻校验，别等用的时候才发现。** 少了牌义的牌在界面上是一片空白，
抽到才发现——所以在生成阶段就拦：

```js
if (cards.length !== 78) errors.push(`牌数应为 78，实际 ${cards.length}`);
const expect = { major: 22, wands: 14, cups: 14, swords: 14, pentacles: 14 };
// 每张牌的正位/逆位都必须有 meaning 和 keywords，缺一个就报
// 牌阵的位置标签数量必须等于牌数（三张牌阵配三个标签）
```

---

## 三、存引用，不要存副本

抽牌记录该存什么？直觉是把牌面整个存下来，以后读历史直接显示。**不要这么做。**

```js
// 存进库的（cards 字段，JSON）：
[{ "id": "major_16", "reversed": true }, { "id": "cups_11", "reversed": false }]

// 不存：牌名、牌义、关键词——一个字都不存
```

理由：**牌义是会被改写的。** 存了副本，等哪天把某张牌的说法写得更好，
历史记录里还是旧的那套——同一张牌，图鉴里一个说法，牌记里另一个说法，
**数据自己跟自己打架**。存 id，读的时候现补，永远一致。

这条推广开来就是：**凡是「主数据可能被编辑」的场景，历史记录都该存引用而不是快照。**
（反过来，订单金额、签署过的合同条款那种「就是要冻结当时状态」的，才该存快照。
判据是问一句：主数据变了，历史应该跟着变吗？）

补全的时候要留退路——牌库里删掉过的牌，不能让整页历史打不开：

```js
function hydrate(list) {
  return list.map(({ id, reversed }) => {
    const card = cardsById.get(id);
    if (!card) return { id, nameCn: '（这张牌已不在牌库里）', reversed: !!reversed, missing: true };
    return { ...card, ...card[reversed ? 'reversed' : 'upright'], reversed };
  });
}
```

同理，一条记录的 JSON 坏了也不该连累整页：

```js
let cards = [];
try { cards = hydrate(JSON.parse(draw.cards)); } catch { cards = []; }
```

### 位置名也只写一份

「过去 / 现在 / 未来」这种牌阵位置标签，前端要用、AI 工具要用、历史记录要用。
写三份的结果是改牌阵时只改得到一处。让它跟着牌库走：

```js
function labelsFor(spread) {
  const labels = deck.spreads?.[String(spread)]?.labels;
  return Array.isArray(labels) ? labels : [];
}
```

前端保留一份兜底常量，但只在服务端没给的时候用，且明确注释说明它是兜底不是主源。

### 随机数

用 `crypto.randomInt`，不用 `Math.random`。Fisher-Yates 洗牌保证不重复：

```js
const shuffled = deck.cards.map((c) => c.id);
for (let i = shuffled.length - 1; i > 0; i -= 1) {
  const j = crypto.randomInt(i + 1);
  [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
}
return shuffled.slice(0, n).map((id) => ({ id, reversed: crypto.randomInt(100) < 40 }));
```

**凡是用户会当真的随机，都值得用真随机源。** 成本几乎为零，而「这东西是不是在骗我」
一旦被怀疑就很难挽回。

验收也别靠肉眼——跑三千次，看统计量：

```
3000 次五张抽牌 → 40.4% 逆位（期望 40%）、0 次牌内重复、78 张牌全部出现过
```

---

## 四、暗道：给模型看的比给人看的多

这是整个功能的核心交互。她点「让 AI 解牌」之后，发进对话的东西分成两份：

**她的气泡里显示的**——只有牌面：

```
🔮 我抽了一手牌 · 时光之流
【过去】星币王牌（逆位）　错失 · 不稳 · 缺乏规划 · 短视
【现在】圣杯侍从　消息 · 浪漫 · 直觉 · 新感受
【未来】…

想问的是：下个月会发生好事吗？

帮我解一下？
```

**模型额外收到的**（她看不见）——三张牌的完整牌义正文，加上一句怎么解的指示。

实现上是消息对象上一个 `injected` 字段，渲染时不显示，送给模型时拼上去：

```js
// 渲染：只用 content
// 送模型：
content: m.injected ? `${m.content}\n\n${m.injected}` : m.content
```

为什么要这样：五张牌阵的完整牌义有一屏那么长。全铺在她的气泡里，
**她想看的「我抽到了什么」会被淹没在「这些牌是什么意思」里**——
而后者本来就是她让 AI 去读的东西。

> 一般化：**当用户请求 AI 处理某样东西时，「原始材料」和「用户的意图表达」
> 是两种不同的东西。** 前者该给模型，后者才该显示。硬把两者塞进同一条消息，
> 界面和提示词会互相拖累。

顺带一提，让 AI 把解读**写回**那条记录（工具调用带上 `id`），历史里就会留下
「这次抽到什么 + 当时怎么解的」。以后能自然地说出「上次你抽到高塔那次」。

---

## 五、界面：横滑是设计，竖滑是 bug

三张、五张牌阵在手机上塞不下，做了横向滚动 + `scroll-snap`。这是有意的。

上线后的反馈是：「左右滑动没问题，但是还要上下滑动」。

这句话很精确——**同样是滚动，一个是设计，一个是缺陷**。区别在于用户是否预期它。
横滑有牌堆的隐喻，滑得心安理得；竖滑意味着「这一屏没装下」，每次都要找按钮在哪。

原因是牌高写死了 `min-height: 200px`。加上标题栏、页签、箴言、牌阵选择、
提问框、两排按钮，一屏就超了。改成跟着视口走：

```css
/* 用 min-height 不是 height —— 点开「看牌义」时牌还得能长高 */
min-height: clamp(132px, 22vh, 190px);
```

`clamp` 的三个值各有分工：下限保证矮屏上牌不至于挤成一条，`22vh` 让它跟着屏幕缩，
上限防止在平板上撑成巨牌。

一次没调到位——第一版给的 `25vh`，反馈是「就只差一点点」，再削一档才合适。
**这类数值不要指望一次估准，把它做成一次往返就好。**

### 附带抓到的一个 bug：只剩一个 emoji 的按钮

截图里那个按钮只显示一只鸟，文字没了。原因：

```css
.roost-btn   { background: var(--accent); color: #fff; }   /* 填充按钮 */
.fate-go     { color: var(--accent); }                     /* ← 蓝字写在蓝底上 */
```

**emoji 是彩色字形，不受 `color` 影响**，所以只有 emoji 活了下来——
这个特性让 bug 看起来像「文案写错了」而不是「颜色错了」，很能误导人。

这个 CSS 是从另一个功能抄来的，也就是说**那个功能的按钮一直也是瞎的，
只是从没人盯着看**。抄样式的时候顺手体检一下被抄的那份，经常有意外收获。

---

## 小结：能拿走的五条

1. **搬运已有工具前，先把「旧的留不留、什么时候介入、存不存」问清楚**，别顺手升级。
2. **一份数据多方消费时，与其追求完美的单一数据源，不如做「生成 + `--check`」**，
   让漂移变成会报错的事。
3. **历史记录存引用不存副本**——判据是「主数据变了，历史该跟着变吗」。
4. **给模型的材料和用户的意图分开**，用隐藏字段承载前者。
5. **横滑可以是设计，竖滑通常是缺陷**——用户预期决定了同一个行为是特性还是 bug。
