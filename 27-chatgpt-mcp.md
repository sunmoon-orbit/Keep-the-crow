# 27 — 把自建 MCP 接进 ChatGPT：连接成功不等于当前窗口能用

## 这是什么

把已经跑在服务器上的 MCP 接进 ChatGPT，让聊天窗口直接调用 `check_health`、记忆检索、共读等自建工具。

这篇不重复讲具体业务接口，而是记录一次真实接入里最容易误判的几层状态：**进程在线、MCP 可连接、工具元数据已刷新、当前对话已加载工具、实际调用成功**。它们不是一回事。

```
业务 API / SQLite
      ↑
MCP Server（SSE 或 Streamable HTTP）
      ↑ HTTPS
ChatGPT 中的开发者模式 App
      ↑
新对话显式启用 App
```

OpenAI 当前文档中的入口名称可能显示为 Plugins、Apps 或开发者模式 App；界面文案会变化，判断标准始终是：能填远程 MCP 地址、发现工具列表，并能在对话里启用它。

---

## 一、接入前先证明服务器这一侧真的正常

ChatGPT 只负责连接远程 MCP，不会替你启动服务器。先在 VPS 上分层检查：

```bash
pm2 status
pm2 logs moon-memory --lines 100
```

重点看：

- 进程的 `status` 是不是 `online`
- 日志里有没有启动失败、端口占用、路由 404、鉴权失败
- MCP 地址是否能从公网访问，而不只是服务器本机能访问
- 公网方案是否使用 HTTPS
- URL 是否包含实际 MCP 路径，常见为 `/mcp`

PM2 表格里的颜色和重启次数不是最终结论。某一格变红不等于服务已经挂了；以 `status`、日志和实际请求为准。

如果改过代码或环境变量：

```bash
pm2 restart moon-memory
pm2 save
```

`pm2 restart` 只说明进程被重新拉起，不能证明 MCP 初始化和工具调用都成功，后面仍要做端到端验证。

---

## 二、在 ChatGPT 中添加远程 MCP

1. 打开 **设置 → 安全与登录**，开启开发者模式。
2. 进入 ChatGPT 的 Plugins / Apps 管理页，点击加号创建开发者模式 App。
3. 填写用户可辨认的名称和说明。
4. 选择连接方式并填写远程 MCP 地址；公网地址应是稳定的 HTTPS 端点，并带上正确的 `/mcp` 路径。
5. 创建后检查发现到的工具名称、说明、参数和注解。
6. 回到对话，在工具菜单或开发者模式里显式启用这个 App。

官方当前支持 SSE 和 Streamable HTTP。鉴权可用 OAuth、无鉴权或混合鉴权；但“平台允许无鉴权”不等于“私人数据适合裸奔”。健康、记忆、日记等个人数据至少应放在受控网络、Secure MCP Tunnel 或可靠鉴权之后，绝不要把 token 塞进 URL。

---

## 三、今天踩到的核心坑：旧窗口看不到，新窗口立刻正常

真实现象：

1. MCP 服务已用 PM2 重启，进程显示在线。
2. ChatGPT 里的连接也已经存在。
3. 原来的长对话仍然查不到新工具或数据。
4. 新开一个对话、重新选择 App 后，`check_health` 立即返回了新鲜快照。

这说明**当前对话已经拿到的工具集合，可能不会因为服务重启或连接重建而自动更新**。排查顺序应是：

1. 部署或重启 MCP 服务。
2. 到 App 详情页点击 **Refresh / 刷新**，重新拉取工具元数据。
3. 确认工具列表里已经出现目标工具。
4. **新建对话**。
5. 在新对话中重新启用该 App。
6. 发一个明确的最小测试请求。

不要在一个已经很长、加载又慢的旧窗口里无限重试。刷新网页、重连 MCP 和新建对话解决的不是同一层问题；其中任何一步成功，都不能替代最后的真实工具调用。

---

## 四、最小端到端测试

不要只问“连上了吗”，直接调用一个只读工具。例如：

> 只使用「小鹿的健康」里的 `check_health`，读取最近 1 小时、最多 3 条记录，并告诉我最新快照的北京时间与数据新鲜度。

验收时同时看：

- ChatGPT 是否选中了预期工具
- 参数是否正确
- 服务端是否收到调用
- 返回是否包含业务数据，而不只是 `200 OK`
- 时间是否按 UTC 正确换算
- 数据是否 fresh；旧快照只能证明“历史上报过”，不能证明当前管线仍活着

这次成功样本里，最新快照只有约 1 分钟延迟，才算真正证明“Tasker 上报 → 数据库 → MCP → ChatGPT”整条链路通了。

建议再补三类测试：

- **直接请求**：明确点名工具
- **自然请求**：只说“看看我今天睡得怎么样”
- **负向请求**：要求一个服务不支持的操作，确认模型不会乱选相似工具

---

## 五、服务能用后，终端能不能关

分两种情况：

- 直接运行 `node server.js`、`npm start`：进程依附当前终端，关终端通常就停。
- 由 PM2 托管且状态为 `online`：可以关 SSH 或终端，服务继续在后台运行。

最可靠的验证不是猜：

1. 关闭终端。
2. 换一个客户端或新对话再次调用只读工具。
3. 等下一个上报周期，确认最新时间继续向前走。

这次关掉终端后，ChatGPT 仍然能读取健康快照，证明服务确实由 PM2 后台托管。执行 `pm2 save` 还能保存当前进程清单；要实现 VPS 重启后自动恢复，仍需确认 PM2 的开机启动已经配置完成。

---

## 六、工具元数据也会制造“像是连接坏了”的错觉

工具能被发现，但模型不一定会正确选择。建议：

- 工具名短而明确，一个工具只做一件事
- description 写清“什么时候用”和“不该什么时候用”
- 参数补齐描述、枚举与默认值
- 只读工具标 `readOnlyHint: true`
- 写操作在服务端继续做鉴权、确认和审计，不能只信模型
- 修改工具名、schema、description、annotations 后：**重启服务 → Refresh App → 新建对话复测**

ChatGPT 会把没有 `readOnlyHint` 的工具按写操作处理，因此一个明明只读的健康查询如果漏了注解，可能反复弹确认，看起来像工具不好用。

---

## 七、故障对照表

| 现象 | 更可能是哪一层 | 先做什么 |
| --- | --- | --- |
| 添加 App 时就连接失败 | HTTPS、路径、传输、初始化或鉴权 | 用 MCP Inspector 查端点，核对 `/mcp` 和日志 |
| PM2 显示 online，但 ChatGPT 没工具 | 元数据尚未刷新 | App 详情页 Refresh |
| App 详情页有工具，当前对话没有 | 当前窗口未重新加载 App | 新建对话并重新启用 |
| 工具能调用，但返回旧数据 | 上游 Tasker/采集链路停了 | 看快照时间与 freshness |
| 只读查询总要求确认 | 缺少 `readOnlyHint` | 修注解、重启、Refresh、新窗口 |
| 关终端后服务消失 | 进程以前台方式运行 | 改用 PM2，再做关闭终端复测 |
| 改了工具说明但行为没变 | 旧元数据或旧对话缓存 | 重启 → Refresh → 新对话 |
| 明明有工具却总选错 | 工具名/描述含糊或多个 App 重叠 | 改描述，测试时明确点名 App 与工具 |

---

## 八、最短可靠流程

```
本机/Inspector 调通
→ PM2 托管并看日志
→ HTTPS /mcp 可达
→ ChatGPT 创建开发者模式 App
→ 检查工具列表
→ Refresh
→ 新建对话并启用 App
→ 调只读工具
→ 核对业务数据时间与新鲜度
→ 关终端后再测一次
```

连接成功只是中间状态。只有最后一条真实数据穿过整条链路，才叫接好了。

## 参考文档

- [OpenAI：ChatGPT Developer mode](https://developers.openai.com/api/docs/guides/developer-mode)
- [OpenAI：Connect and test your plugin](https://developers.openai.com/plugins/deploy/connect-chatgpt)
